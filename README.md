# Containers on Azure Demo

## Kubernetes Notes

```
az aks install-cli
az aks get-credentials --resource-group <resource group name> --name <cluster name>
```

url: http://ffhs-acc-aks.northeurope.cloudapp.azure.com/
